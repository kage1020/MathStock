---
title: 1984年
description: 東京科学大学(東京工業大学)の過去問 1984年
tableOfContents: false
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import { Card } from '@/components/Card'
import { List, ListItem } from '@/components/List'
import { Image } from '@/components/Image'

<Tabs>
  <TabItem class="overflow-x-auto" label="問題">
    <Card>
      $n$ を3以上の整数とする．条件

      $$
        x+y+z=n,\ x\leqq y+z,\ y\leqq z+x,\ z\leqq x+y
      $$

      を満たす正の整数の組 $(x,\ y,\ z)$ の個数を求めよ。
    </Card>

  </TabItem>
  <TabItem label="解答">
    $$
    \begin{align}
        x+y+z&=n,\ x\leqq y+z,\ y\leqq z+x,\ z\leqq x+y\tag{1}\\
        z&=n-(x+y)\tag{2}
    \end{align}
    $$

    (2)式を $x\leqq y+z$，$y\leqq z+x$，$z\leqq x+y$ に代入すると，

    $$
    \begin{align}
        &x\leqq y+n-(x+y),\ y\leqq n-(x+y)+x,\ n-(x+y)\leqq x+y\nonumber\\
        &\therefore x\leqq \frac{1}{2}n,\ y\leqq \frac{1}{2}n,\ x+y\geqq \frac{1}{2}n\tag{3}
    \end{align}
    $$

    また，$x>0$, $y>0$, $z>0$ より

    $$
    \begin{equation}
        x>0,\ y>0,\ n-(x+y)>0\tag{4}
    \end{equation}
    $$

    (3)式，(4)式を満たす正の整数の組 $(x,\ y)$ に対して，(2)式によって $z$ を定めれば，正の整数の組 $(x,\ y,\ z)$ は(1)式を満たす．逆も成り立つから，(1)式を満たす正の整数の組 $(x,\ y,\ z)$ の個数は(3)式かつ(4)式を満たす正の整数の組 $(x,\ y)$ の個数に等しい．

    <List type="lower-roman">
      <ListItem>
        $n=2k\ (k=2,\ 3,\ldots )$のとき

        (3)式かつ(4)式の表す領域は図の網目部分となる．この領域にあり，$x=m\quad (m=1,\ 2,\ \cdots,\ k-1)$ である格子点は，

        $$
          y=k-m,\ k-m+1,\ \cdots,\ k
        $$

        の $k-(k-m)+1=m+1$ 個である．

        $x=0$ 上の格子点は0個，$x=k$ 上の格子点は $k-1$ 個であるから，求める個数は，
        $$
            \sum_{k=1}^{m-1}(m+1)+k-1=\frac{1}{2}(k+4)(k-1)
        $$
      </ListItem>
      <ListItem>
        $n=2k+1\quad (k=1,\ 2,\ \cdots)$ のとき

        (i)と同様に考え，$x=m\quad (m=1,\ 2,\ \cdots,\ k)$である格子点は，

        $$
            y=k+1-m,\ k+2-m,\ \cdots,\ k
        $$

        の $k-(k+1-m)+1=m$ 個である．求める個数は，

        $$
            \displaystyle\sum_{m=1}^{k}m=\frac{1}{2}k(k+1)
        $$
      </ListItem>
    </List>

    以上をまとめて，求める個数は，

    $$
    \left\{\begin{aligned}
        n\text{が偶数のとき}&\quad\frac{1}{2}(k+4)(k-1)=\frac{1}{8}(n+8)(n-2)\\
        n\text{が奇数のとき}&\quad\frac{1}{2}k(k+1)=\frac{1}{8}(n-1)(n+1)
    \end{aligned}\right.
    $$

    である。

    <Image src={["/past-exams/titech/1984/f1_light.svg","/past-exams/titech/1984/f1_dark.svg"]} caption="(i)" />
    <Image src={["/past-exams/titech/1984/f2_light.svg","/past-exams/titech/1984/f2_dark.svg"]} caption="(ii)" />

  </TabItem>
</Tabs>
